# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# ⚠️ DEVELOPMENT MODE - NAIVE AUTH ENABLED
# Email verification is disabled for development to bypass rate limits
# See NAIVE_AUTH_SETUP.md for production configuration

# Supabase URL - found in Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon/Public Key - found in Settings > API
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service Role Key for server-side operations (keep this secret!)
# Only use this in server-side code, never expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Database URL for direct connections (if needed)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Application Settings
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Maps API Key (optional, for geocoding and maps)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Optional: Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional: Error Tracking
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn.ingest.sentry.io/project-id

# Development Settings
NODE_ENV=development
